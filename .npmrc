# Hoisted (flat) node_modules. Avoids pnpm symlink edge cases with Next.js + Tailwind
# JIT scanning + workspace TS packages. Not strictly required, but it keeps the dev
# experience predictable and mirrors the backend repo's setup.
node-linker=hoisted
